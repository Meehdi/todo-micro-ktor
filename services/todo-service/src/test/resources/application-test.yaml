ktor:
  application:
    modules:
      - com.meehdi.ApplicationKt.module
  deployment:
    port: 8080
    host: 0.0.0.0

mongodb:
  uri: mongodb://localhost:27017
  database: test_db
  collection: todos

kafka:
  bootstrap-servers: localhost:9092
  topic: todo-events-test
  group-id: todo-service-test
  client-id: todo-producer-test