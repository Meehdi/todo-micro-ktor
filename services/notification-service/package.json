{
  "name": "notification-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "./gradlew build -x test",
    "docker:build": "echo 'Building notification-service docker image'"
  }
}